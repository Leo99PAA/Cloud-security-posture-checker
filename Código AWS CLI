import typer
from <PACKAGE_NAME>.<MODULE_NAME> import <SCAN_FUNCTION_NAME>

app = typer.Typer(help="Cloud Posture Checker (MVP)")

@app.command()
def scan(
    profile: str = typer.Option("<AWS PROFILE>", help="AWS CLI profile  <AWS PROFILE>"),
    region: str = typer.Option("<AWS REGIÃO>", help="Região padrão <AWS REGIÃO>"),
):
    scan_teste(profile=profile, region=region)
if __name__ == "__main__":
    app()
