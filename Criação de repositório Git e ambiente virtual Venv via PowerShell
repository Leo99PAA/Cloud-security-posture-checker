mkdir cloud-posture-checker                                                                   (crian uma pasta para servir de repositório dentro do terminal de escolha) 
cd cloud-posture-checker                                                                      (especifica para o power shell mudar a pasta atual pela aque foi criada anteriomente)
git init                                                                                      (transforma a pasta em um repositório Git)
py -3.14 -m venv .venv                                                                        (Cria um ambiente virtual usando o python 3.14)
#em caso de erro por "não pode ser carregado porque a execução de scripts foi desabilitada neste sistema." usar o comando: Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
.\.venv\Scripts\Activate.ps1                                                                  (ativa o ambiente virtual para os comados apontarem para o pyhton dentro da .venv)
py -3.14 -m pip install --upgrade pip                                                         (instala o pip da versão 3.14 que foi utilizada na venv, e garante a atualização do mesmo)
pip install boto3 typer rich pydantic                                                         (Instala as bibliotecas necessárias atraves do gerenciador de pacotes pip)
pip freeze > bibliotecasCSPM.txt                                                              (salva um arquivo txt para se for necessário instalar as bibliotecas no futuro usar somente o comando: pip install -r bibliotecasCSPM.txt)
mkdir src                                                                                     (cria a pasta aonde vai ficar o código fonte)
mkdir src\cspm_checker                                                                        (cria a pasta aonde vai ficar os pacotes python)
new-item src\cspm_checker\__init__.py -itemtype file                                          (Cria um arquivo python vazio, que é útil para imports)
new-item src\cspm_checker\cli.py -itemtype file                                               (cria a pasta aonde vai ficar a interface de linha de comando)
new-item src\cspm_checker\scan_teste.py -itemtype file                                        (cria o arquivo onde vai ficar o primeiro teste de escaneamento)
new-item .gitignore -itemtype file                                                            (cria o arquivo aonde vamos expecificar ao git quais pastas não devem ser versionadas)
